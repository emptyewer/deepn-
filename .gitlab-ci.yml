image:
  name: venkykrishna/deepn:latest
  entrypoint: ["bash", "/entrypoint.sh"]

stages:
   - linux-win-build

build_jobs:
   stage: linux-win-build
   script:
      -  docker run -v $(pwd):/workdir venkykrishna/deepn:latest
