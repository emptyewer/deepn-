stages:
   - base
   - linux-stage1
   - linux-stage2
   - linux-stage3
   - docker
   - build
   - publish

variables:
  DOCKER_HOST: tcp://docker:2375/
  DOCKER_DRIVER: overlay2

base-docker:
   stage: base
   image: docker:latest
   services:
     - docker:dind
   only:
      changes:
        - docker/linux/base/Dockerfile
   script:
      - docker login -u $CI_DEPLOY_USER -p $CI_DEPLOY_PASSWORD $CI_REGISTRY
      - docker build . --rm --no-cache -t $CI_REGISTRY/emptyewer/deepn-plus/base:latest -f docker/linux/base/Dockerfile
      - docker push $CI_REGISTRY/emptyewer/deepn-plus/base:latest

linux-docker-stage1:
   stage: linux-stage1
   image: docker:latest
   services:
     - docker:dind
   only:
      changes:
        - docker/linux/base/Dockerfile
        - docker/linux/stage1/Dockerfile
   dependencies:
     - base-docker
   script:
      - docker login -u $CI_DEPLOY_USER -p $CI_DEPLOY_PASSWORD $CI_REGISTRY
      - docker build . --rm --no-cache -t $CI_REGISTRY/emptyewer/deepn-plus/linux64-stage1:latest -f docker/linux/stage1/Dockerfile
      - docker push $CI_REGISTRY/emptyewer/deepn-plus/linux64-stage1:latest

linux-docker-stage2:
   stage: linux-stage2
   image: docker:latest
   services:
     - docker:dind
   only:
      changes:
        - docker/linux/base/Dockerfile
        - docker/linux/stage1/Dockerfile
        - docker/linux/stage2/Dockerfile
   dependencies:
     - base-docker
     - linux-docker-stage1
   script:
      - docker login -u $CI_DEPLOY_USER -p $CI_DEPLOY_PASSWORD $CI_REGISTRY
      - docker build . --rm --no-cache -t $CI_REGISTRY/emptyewer/deepn-plus/linux64-stage2:latest -f docker/linux/stage2/Dockerfile
      - docker push $CI_REGISTRY/emptyewer/deepn-plus/linux64-stage2:latest

linux-docker-stage3:
   stage: linux-stage3
   image: docker:latest
   services:
     - docker:dind
   only:
      changes:
        - docker/linux/base/Dockerfile
        - docker/linux/stage1/Dockerfile
        - docker/linux/stage2/Dockerfile
        - docker/linux/stage3/Dockerfile
   dependencies:
     - base-docker
     - linux-docker-stage1
     - linux-docker-stage2
   script:
      - docker login -u $CI_DEPLOY_USER -p $CI_DEPLOY_PASSWORD $CI_REGISTRY
      - docker build . --rm --no-cache -t $CI_REGISTRY/emptyewer/deepn-plus/linux64-stage3:latest -f docker/linux/stage3/Dockerfile
      - docker push $CI_REGISTRY/emptyewer/deepn-plus/linux64-stage3:latest

linux-docker:
   stage: docker
   image: docker:latest
   services:
     - docker:dind
   only:
      changes:
        - docker/linux/base/Dockerfile
        - docker/linux/stage1/Dockerfile
        - docker/linux/stage2/Dockerfile
        - docker/linux/Dockerfile
        - docker/linux/default
        - docker/linux/entrypoint.sh
   dependencies:
     - base-docker
     - linux-docker-stage1
     - linux-docker-stage2
   script:
      - docker login -u $CI_DEPLOY_USER -p $CI_DEPLOY_PASSWORD $CI_REGISTRY
      - docker build . --rm --no-cache -t $CI_REGISTRY/emptyewer/deepn-plus/linux64:latest -f docker/linux/Dockerfile
      - docker push $CI_REGISTRY/emptyewer/deepn-plus/linux64:latest

windows-docker:
   stage: docker
   image: docker:latest
   services:
     - docker:dind
   only:
      changes:
        - docker/windows/Dockerfile
        - docker/windows/default
        - docker/windows/entrypoint.sh
   script:
      - docker login -u $CI_DEPLOY_USER -p $CI_DEPLOY_PASSWORD $CI_REGISTRY
      - docker build . --rm --no-cache -t $CI_REGISTRY/emptyewer/deepn-plus/windows64:latest -f docker/windows/Dockerfile
      - docker push $CI_REGISTRY/emptyewer/deepn-plus/windows64:latest

linux-build:
   stage: build
   image: emptyewer/deepn-plus/linux64:latest
   before_script: []
   script:
      -  docker login -u $CI_DEPLOY_USER -p $CI_DEPLOY_PASSWORD $CI_REGISTRY
      -  docker run $CI_REGISTRY/emptyewer/deepn-plus/linux64:latest
   dependencies:
      - linux-docker
   artifacts:
      paths:
         - /builds/emptyewer/deepn-plus/deepn++_linux_64bit.7z
      expire_in: '1 hour'

windows-build:
   stage: build
   image: emptyewer/deepn-plus/windows64:latest
   before_script: []
   script:
      - docker login -u $CI_DEPLOY_USER -p $CI_DEPLOY_PASSWORD $CI_REGISTRY
      - docker run $CI_REGISTRY/emptyewer/deepn-plus/windows64:latest
   dependencies:
      - windows-docker
   artifacts:
      paths:
         - /builds/emptyewer/deepn-plus/deepn++_windows_64bit.7z
      expire_in: '1 hour'

publish:
   image: venkykrishna/gitlab-release
   stage: publish
   dependencies:
      - linux-build
      - windows-build
   before_script: []
   script:
        - gitlab-release --message "Latest Release ($(date))" /builds/emptyewer/deepn-plus/deepn++_linux_64bit.7z /builds/emptyewer/deepn-plus/deepn++_windows_64bit.7z
